{
  "name": "signal-ai-assistant",
  "version": "0.0.1",
  "description": "An LLM-powered Signal assistant",
  "main": "src/main.ts",
  "scripts": {
    "dev": "ts-node src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "vitest run",
    "prepare": "husky"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "dotenv": "^16.0.3",
    "typescript": "^5.0.4",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "husky": "^9.1.7",
    "ts-node": "^10.9.1",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
